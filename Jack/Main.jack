class Countdown {
    function void start(int startNumber) {
        var int i;

        let i = startNumber;
        while (i > 0) {
            do Output.printInt(1 + (2 * 3));
            let i = i - 1; // Decrement i
        }
        return;
    }
}